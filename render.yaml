services:
  - type: web
    name: avalon-online-private
    runtime: node
    plan: free
    branch: server
    buildCommand: echo "No build step"
    startCommand: node server.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
